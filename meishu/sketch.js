function setup() {
 canvastest = createCanvas(400, 400);
//var canvastest = createCanvas(500, 500);
 
  // Move the canvas so it¡¯s inside our <div id="sketch-holder">.
  //canvastest.parent('sketch-holder');

  background(255, 0, 200);
}